folds <- as.numeric(sample(rep(1:4,length.out = length(12))))
knitr::opts_chunk$set(echo = TRUE)
folds <- as.numeric(sample(rep(1:k,length.out = length(temp))))
folds
folds <- as.numeric(sample(rep(1:k,length.out = length(temp))))
folds <- as.numeric(sample(rep(1:4,length.out = length(12))))
folds
folds <- as.numeric(sample(rep(1:4,length.out = 12))
)
folds
sort(folds)
